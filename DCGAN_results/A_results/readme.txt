For this folder, the program is trained ONLY on the A subfolder, so only A is generated by the Generator G model. 
Except for the generated images, there are also corresponding saved loss plots.

To save memory, saved trained models are omitted here as they are too large. 
